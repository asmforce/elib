// Сборка
make all
Сборка библиотек и демо-приложений. Копирование библиотеки
в %SystemRoot%\system32\elib.dll (win32), или /usr/lib/libelib.so (linux);

make clean
Полная очистка всех сборок (удаление
объектных, исполняемых файлов и библиотек);

make sub-clean
Очистка сборок в ./demo и ./src;


// Соглашения
- Все приложения, использующие библиотеку elib, должны
использовать беззнаковый символьный тип
(собираться с ключем `-funsigned-char`для gcc/mingw);
